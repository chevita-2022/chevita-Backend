language: java
jdk:
  - openjdk11

branches:
  only:
    - main
# 모든 사용자에게 접근 권한 풀어주기 -> gradlew permission error
#before_install:
 # - -chmod +x gradlew 오류나서 주석처리 -> git bash로 설정

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradle clean build"

# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - susu19@ewhain.net
      - doooheun@gmail.com